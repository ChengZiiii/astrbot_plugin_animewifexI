# 玩法设计文档归档

> 本目录存放 `astrbot_plugin_animewifexI` 的玩法设计稿与策划案，**不进 git**、**不进 sync.bat 同步到运行端**。
>
> 仅作为设计阶段的草稿与方案讨论用，不影响生产代码。

## 当前目录

| 文件 | 角色 | 状态 |
|------|------|------|
| `玩法升级策划案.md` | v2 基线（连续签到、对话/约会、打工、NTR 补偿、PK 重构） | ✅ 已落地 Phase 4 |

## 设计约定

- 新设计文档请在本目录下新建 `.md` 文件，命名建议带版本号或专题名（如 `v3_4v4编队_离婚_负债.md`）。
- 与代码的对应关系请在 README 主目录的 `CHANGELOG.md` / `ROADMAP.md` 顶部加入口。
- 每次正式落地时，把对应文档移入 `_archived/` 子目录（即将创建），保留可追溯性。
